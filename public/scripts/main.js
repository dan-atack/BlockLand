// Run area. CAUTION: LIVE CODE FOLLOWS!

// First there was The App:

const app = new App();

// Then we check the user status:

checkUserStatus(app);

// Start the App by running its pre-game menu render method:

app.showPreGameMenu();